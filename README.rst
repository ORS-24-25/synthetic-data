First set up poetry if you haven't
    - Install with `sudo apt install python3-poetry`
    - Check it is installed with `poetry --version`
    - Activate virtual env `poetry shell`
    - Install the required packages `poetry install`